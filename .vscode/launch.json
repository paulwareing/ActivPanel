{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch Chrome with file",
      "file": "${workspaceFolder}/log.htm",
      "webRoot": "${workspaceFolder}"
    }
  ]
}